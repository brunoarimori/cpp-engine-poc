#pragma once

namespace Flags {
	namespace Grid {
		const int TILE_SIZE = 16;
	}
	namespace Movement {
		// Default velocity in pixels per second (?)
		const int DEF_VEL = 2;
		// Default animation speed in seconds
		const float DEF_ANIM = 0.2f;
	}
}
